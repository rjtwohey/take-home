import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.take-home-test',
  appName: 'take-home',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
